-- =====================================================================
-- STAGERZ -- Phase 21.3 -- Function / RPC snapshot
-- =====================================================================
-- DESCRIPTIVE SNAPSHOT -- NOT A MIGRATION.
--
-- This file records what the live Supabase project CONTAINS. It is not
-- an instruction to create, alter or apply anything and must never be
-- executed against a database. EVERY line of this file is an SQL
-- comment; verbatim catalog definitions are prefixed "-- | " inside
-- BEGIN/END blocks that carry a server-side SHA-256 of the exact text.
--
-- Project ref     : kbnmkyvbwkuvcklywdhk  (stagerz-foundation-v2-test)
-- Server          : PostgreSQL 17.6
-- Migration epoch : 20260917143322 phase21_3_r5_w1_w3_w4  (43 migrations recorded)
-- Extracted (UTC) : 2026-09-17T19:33:24Z
-- Extraction      : P213-FUNCTIONS-v2, P213-GRANTS-v2 -- read-only catalog SELECTs
--                   (pg_catalog, information_schema, storage.buckets,
--                   supabase_migrations metadata). No application row,
--                   user, Storage object or secret data is included.
-- State           : CURRENT post-remediation epoch (S-1..S-8 and
--                   O-1..O-3 remediated). Historical pre-remediation
--                   state is documented in backend-contract.md only.
-- Generated by    : deterministic renderer, stable ordering, LF only.
--                   See validation.md section 13 for the method and the
--                   repository-to-live fingerprint reconciliation.
-- =====================================================================
--
-- Scope:
--   ALL functions in schema public, with their COMPLETE pg_get_functiondef()
--   text, attributes, EXECUTE grants, RAISE sites and custom SQLSTATEs.
--   Trigger definitions are in triggers.sql; the functions they call are here.
--

-- ---------------------------------------------------------------------
-- 1. Inventory
-- ---------------------------------------------------------------------
-- public functions          : 34  (overloads: 0; kind: all plain functions)
-- by role in the app        : admin 5, frontend RPC 20, helper 5, trigger 4
-- frontend RPCs (index.html): 20 distinct names; all present below
-- SECURITY DEFINER          : 34/34
-- search_path=""            : 34/34
-- owner                     : postgres
-- RAISE EXCEPTION sites     : 121
-- distinct custom SQLSTATEs : 58
-- aggregate sha256          : 1abd299b5e3f39bd5919e522357f2d8e17522ff87421f4560c6bf79c03588d43
--   = sha256 of the newline-joined lines "<name>(<args>):<definition sha256>", ordered by name, args
--
-- function                           | role         | lang    | volatility | security | config         | EXECUTE granted to                  | raises | sha256 (prefix)
-- -----------------------------------+--------------+---------+------------+----------+----------------+-------------------------------------+--------+-----------------
-- admin_anonymize_account            | admin        | plpgsql | VOLATILE   | DEFINER  | search_path="" | postgres,service_role               | 1      | 249247e14970c1a6
-- admin_block_user                   | admin        | sql     | VOLATILE   | DEFINER  | search_path="" | postgres,service_role               | 0      | a7259b5ac0b11447
-- admin_link_recovered_account       | admin        | plpgsql | VOLATILE   | DEFINER  | search_path="" | postgres,service_role               | 2      | 9feacfd11940b781
-- admin_unblock_user                 | admin        | sql     | VOLATILE   | DEFINER  | search_path="" | postgres,service_role               | 0      | 2405de1f6b20e09b
-- admin_unmap_and_orphan_check       | admin        | plpgsql | VOLATILE   | DEFINER  | search_path="" | postgres,service_role               | 2      | a54b3c19c0e765d1
-- change_collaboration_status        | frontend RPC | plpgsql | VOLATILE   | DEFINER  | search_path="" | authenticated,postgres,service_role | 4      | aadcaec005d71aee
-- close_own_wanted_post              | frontend RPC | plpgsql | VOLATILE   | DEFINER  | search_path="" | authenticated,postgres,service_role | 2      | 30143c7184b1f7a3
-- complete_collaboration_task        | frontend RPC | plpgsql | VOLATILE   | DEFINER  | search_path="" | authenticated,postgres,service_role | 5      | d5aa3ae525e70acb
-- create_collaboration_credit        | frontend RPC | plpgsql | VOLATILE   | DEFINER  | search_path="" | authenticated,postgres,service_role | 7      | f06bf5a3de37878b
-- create_collaboration_message       | frontend RPC | plpgsql | VOLATILE   | DEFINER  | search_path="" | authenticated,postgres,service_role | 6      | 15b22ff0ccd3b25e
-- create_collaboration_task          | frontend RPC | plpgsql | VOLATILE   | DEFINER  | search_path="" | authenticated,postgres,service_role | 7      | 7d6e6b83132c0e40
-- create_wanted_application          | frontend RPC | plpgsql | VOLATILE   | DEFINER  | search_path="" | authenticated,postgres,service_role | 4      | c388a0a40c4ca3d4
-- current_active_stagerz_user_id     | helper       | sql     | STABLE     | DEFINER  | search_path="" | authenticated,postgres,service_role | 0      | 90dad6cd09a08c5f
-- current_stagerz_user_id            | helper       | sql     | STABLE     | DEFINER  | search_path="" | authenticated,postgres,service_role | 0      | 18146d74acbab9bc
-- delete_collaboration_asset         | frontend RPC | plpgsql | VOLATILE   | DEFINER  | search_path="" | authenticated,postgres,service_role | 6      | 760b735f77b29434
-- delete_collaboration_credit        | frontend RPC | plpgsql | VOLATILE   | DEFINER  | search_path="" | authenticated,postgres,service_role | 5      | 68b70e3e66ebbbdd
-- delete_collaboration_message       | frontend RPC | plpgsql | VOLATILE   | DEFINER  | search_path="" | authenticated,postgres,service_role | 6      | 076d9348e7d9f810
-- delete_collaboration_task          | frontend RPC | plpgsql | VOLATILE   | DEFINER  | search_path="" | authenticated,postgres,service_role | 5      | b8b793d5123c31ff
-- edit_collaboration_asset           | frontend RPC | plpgsql | VOLATILE   | DEFINER  | search_path="" | authenticated,postgres,service_role | 5      | b85a2758c037599d
-- edit_collaboration_credit          | frontend RPC | plpgsql | VOLATILE   | DEFINER  | search_path="" | authenticated,postgres,service_role | 5      | 48ce55b7e4cc3f0d
-- edit_collaboration_message         | frontend RPC | plpgsql | VOLATILE   | DEFINER  | search_path="" | authenticated,postgres,service_role | 8      | 748e09a65f521b26
-- edit_collaboration_task            | frontend RPC | plpgsql | VOLATILE   | DEFINER  | search_path="" | authenticated,postgres,service_role | 6      | bfc02c49ab6cd70b
-- handle_new_auth_user               | trigger      | plpgsql | VOLATILE   | DEFINER  | search_path="" | postgres,service_role               | 0      | f7fd457bdae8a595
-- has_completed_onboarding           | helper       | sql     | STABLE     | DEFINER  | search_path="" | authenticated,postgres,service_role | 0      | 0cd9aa3ce9972aeb
-- invite_collaboration_participant   | frontend RPC | plpgsql | VOLATILE   | DEFINER  | search_path="" | authenticated,postgres,service_role | 8      | fe48a649028d3542
-- is_collaboration_owner             | helper       | sql     | STABLE     | DEFINER  | search_path="" | authenticated,postgres,service_role | 0      | 17eb83da9b7e1490
-- is_collaboration_participant       | helper       | sql     | STABLE     | DEFINER  | search_path="" | authenticated,postgres,service_role | 0      | c3804d9c1ca3817e
-- leave_collaboration                | frontend RPC | plpgsql | VOLATILE   | DEFINER  | search_path="" | authenticated,postgres,service_role | 5      | 0a04fff28d544426
-- log_collaboration_asset_activity   | trigger      | plpgsql | VOLATILE   | DEFINER  | search_path="" | postgres,service_role               | 0      | bf674957d3cc3603
-- log_collaboration_credit_activity  | trigger      | plpgsql | VOLATILE   | DEFINER  | search_path="" | postgres,service_role               | 0      | 0ba25ff2bd06b301
-- log_collaboration_message_activity | trigger      | plpgsql | VOLATILE   | DEFINER  | search_path="" | postgres,service_role               | 0      | f757a21b5dad8320
-- remove_collaboration_participant   | frontend RPC | plpgsql | VOLATILE   | DEFINER  | search_path="" | authenticated,postgres,service_role | 8      | ef5503e57bb02012
-- respond_to_wanted_application      | frontend RPC | plpgsql | VOLATILE   | DEFINER  | search_path="" | authenticated,postgres,service_role | 5      | 8e25e036a97d7f39
-- transfer_collaboration_ownership   | frontend RPC | plpgsql | VOLATILE   | DEFINER  | search_path="" | authenticated,postgres,service_role | 9      | d011a5526ee3e058

-- ---------------------------------------------------------------------
-- 2. Complete definitions (alphabetical)
-- ---------------------------------------------------------------------
--
-- ===== public.admin_anonymize_account(p_public_user_id uuid) =====
--   returns            : TABLE(pending_auth_user_id uuid)
--   role in the app    : admin
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres}
--   EXECUTE            : postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 917 bytes, md5 e9aa6463c3a2a718ec1853eb3346d9bf
--   RAISE sites        : 1  -> P0007 target_not_found
-- >>> BEGIN function public.admin_anonymize_account(p_public_user_id uuid)
-- >>> sha256 249247e14970c1a62099a35ec086588cb805b274b0c4cc45adf201a238b6f8f9
-- | CREATE OR REPLACE FUNCTION public.admin_anonymize_account(p_public_user_id uuid)
-- |  RETURNS TABLE(pending_auth_user_id uuid)
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | begin
-- |   if not exists (select 1 from public.users where id = p_public_user_id) then
-- |     raise exception 'target_not_found' using errcode = 'P0007';
-- |   end if;
-- |
-- |   update public.users
-- |     set anonymized_at = coalesce(anonymized_at, now()),
-- |         username = null, first_name = null, last_name = null,
-- |         photo_url = null, bio = null, location = null
-- |     where id = p_public_user_id;
-- |
-- |   update public.profiles
-- |     set display_name = 'Deleted User', role = '', category = null,
-- |         skills = '{}', looking_for = '{}', location = '', country_flag = ''
-- |     where user_id = p_public_user_id;
-- |
-- |   return query
-- |     select auth_user_id from public.user_auth_accounts where public_user_id = p_public_user_id;
-- | end;
-- | $function$
-- |
-- <<< END function public.admin_anonymize_account(p_public_user_id uuid)
--
-- ===== public.admin_block_user(p_public_user_id uuid) =====
--   returns            : void
--   role in the app    : admin
--   language           : sql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres}
--   EXECUTE            : postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 258 bytes, md5 869b36a615752d305bb1edf342d11eb0
--   RAISE sites        : 0
-- >>> BEGIN function public.admin_block_user(p_public_user_id uuid)
-- >>> sha256 a7259b5ac0b11447fcee58b7439a1465ae1b933d2834009e9219f8062d0c7068
-- | CREATE OR REPLACE FUNCTION public.admin_block_user(p_public_user_id uuid)
-- |  RETURNS void
-- |  LANGUAGE sql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- |   update public.users set blocked = true, updated_at = now() where id = p_public_user_id;
-- | $function$
-- |
-- <<< END function public.admin_block_user(p_public_user_id uuid)
--
-- ===== public.admin_link_recovered_account(p_auth_user_id uuid, p_public_user_id uuid) =====
--   returns            : void
--   role in the app    : admin
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres}
--   EXECUTE            : postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 693 bytes, md5 45a4b547739fc466131f2d790f3d00de
--   RAISE sites        : 2  -> P0004 already_mapped; P0005 target_not_found_or_anonymized
-- >>> BEGIN function public.admin_link_recovered_account(p_auth_user_id uuid, p_public_user_id uuid)
-- >>> sha256 9feacfd11940b78108725a6ecb4ac84028baca96bbe02d6a33778a20fd185b7b
-- | CREATE OR REPLACE FUNCTION public.admin_link_recovered_account(p_auth_user_id uuid, p_public_user_id uuid)
-- |  RETURNS void
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | begin
-- |   if exists (select 1 from public.user_auth_accounts where auth_user_id = p_auth_user_id) then
-- |     raise exception 'already_mapped' using errcode = 'P0004';
-- |   end if;
-- |   if not exists (select 1 from public.users where id = p_public_user_id and anonymized_at is null) then
-- |     raise exception 'target_not_found_or_anonymized' using errcode = 'P0005';
-- |   end if;
-- |   insert into public.user_auth_accounts (auth_user_id, public_user_id)
-- |     values (p_auth_user_id, p_public_user_id);
-- | end;
-- | $function$
-- |
-- <<< END function public.admin_link_recovered_account(p_auth_user_id uuid, p_public_user_id uuid)
--
-- ===== public.admin_unblock_user(p_public_user_id uuid) =====
--   returns            : void
--   role in the app    : admin
--   language           : sql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres}
--   EXECUTE            : postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 261 bytes, md5 42360a1521e711e390ed7728cf25fd08
--   RAISE sites        : 0
-- >>> BEGIN function public.admin_unblock_user(p_public_user_id uuid)
-- >>> sha256 2405de1f6b20e09bd7ad3f14b5371412a5853076226ee586bec51eabba8f2a8b
-- | CREATE OR REPLACE FUNCTION public.admin_unblock_user(p_public_user_id uuid)
-- |  RETURNS void
-- |  LANGUAGE sql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- |   update public.users set blocked = false, updated_at = now() where id = p_public_user_id;
-- | $function$
-- |
-- <<< END function public.admin_unblock_user(p_public_user_id uuid)
--
-- ===== public.admin_unmap_and_orphan_check(p_auth_user_id uuid) =====
--   returns            : void
--   role in the app    : admin
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres}
--   EXECUTE            : postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 771 bytes, md5 bb64a04830d81fa9940e962016bb2543
--   RAISE sites        : 2  -> P0006 mapping_not_found; P0003 cannot_remove_last_credential
-- >>> BEGIN function public.admin_unmap_and_orphan_check(p_auth_user_id uuid)
-- >>> sha256 a54b3c19c0e765d1070f525cfd08909c5d3b7189e3150f43e949bf02cd9b15a5
-- | CREATE OR REPLACE FUNCTION public.admin_unmap_and_orphan_check(p_auth_user_id uuid)
-- |  RETURNS void
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_mapping_count integer;
-- |   v_domain_id uuid;
-- | begin
-- |   select public_user_id into v_domain_id
-- |     from public.user_auth_accounts where auth_user_id = p_auth_user_id;
-- |   if v_domain_id is null then
-- |     raise exception 'mapping_not_found' using errcode = 'P0006';
-- |   end if;
-- |   select count(*) into v_mapping_count
-- |     from public.user_auth_accounts where public_user_id = v_domain_id;
-- |   if v_mapping_count <= 1 then
-- |     raise exception 'cannot_remove_last_credential' using errcode = 'P0003';
-- |   end if;
-- |   delete from public.user_auth_accounts where auth_user_id = p_auth_user_id;
-- | end;
-- | $function$
-- |
-- <<< END function public.admin_unmap_and_orphan_check(p_auth_user_id uuid)
--
-- ===== public.change_collaboration_status(p_collaboration_id uuid, p_new_status text) =====
--   returns            : void
--   role in the app    : frontend RPC
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres,authenticated=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 3426 bytes, md5 496931af6ca124e572ef8975f394f700
--   RAISE sites        : 4  -> P0001 no_active_stagerz_identity; P0016 collaboration_not_found; P0022 only_owner_may_change_status; P0023 invalid_status_transition
-- >>> BEGIN function public.change_collaboration_status(p_collaboration_id uuid, p_new_status text)
-- >>> sha256 aadcaec005d71aee6d2ba5346d5ed9d14331e9c2edab0393bc497fa2f6751587
-- | CREATE OR REPLACE FUNCTION public.change_collaboration_status(p_collaboration_id uuid, p_new_status text)
-- |  RETURNS void
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_caller_id uuid := public.current_active_stagerz_user_id();
-- |   v_current_status text;
-- |   v_activity_type text;
-- |   v_collab_title text;
-- |   v_notif_type text;
-- |   v_notif_title text;
-- |   v_notif_message text;
-- | begin
-- |   if v_caller_id is null then
-- |     raise exception 'no_active_stagerz_identity' using errcode = 'P0001';
-- |   end if;
-- |
-- |   select status, title into v_current_status, v_collab_title from public.collaborations where id = p_collaboration_id;
-- |   if v_current_status is null then
-- |     raise exception 'collaboration_not_found' using errcode = 'P0016';
-- |   end if;
-- |
-- |   if not public.is_collaboration_owner(p_collaboration_id) then
-- |     raise exception 'only_owner_may_change_status' using errcode = 'P0022';
-- |   end if;
-- |
-- |   if not (
-- |     (v_current_status = 'active'    and p_new_status = 'completed') or
-- |     (v_current_status = 'completed' and p_new_status = 'active')    or
-- |     (v_current_status = 'completed' and p_new_status = 'archived')  or
-- |     (v_current_status = 'archived'  and p_new_status = 'completed')
-- |   ) then
-- |     raise exception 'invalid_status_transition' using errcode = 'P0023';
-- |   end if;
-- |
-- |   v_activity_type := case
-- |     when p_new_status = 'completed' and v_current_status = 'active' then 'collaboration_completed'
-- |     when p_new_status = 'active' then 'collaboration_reopened'
-- |     when p_new_status = 'archived' then 'collaboration_archived'
-- |     when p_new_status = 'completed' and v_current_status = 'archived' then 'collaboration_restored'
-- |   end;
-- |
-- |   update public.collaborations
-- |     set status = p_new_status,
-- |         completed_at = case
-- |           when p_new_status = 'completed' and completed_at is null then now()
-- |           else completed_at
-- |         end,
-- |         archived_at = case
-- |           when p_new_status = 'archived' then now()
-- |           else null
-- |         end
-- |     where id = p_collaboration_id;
-- |
-- |   insert into public.collaboration_activity (collaboration_id, actor_user_id, activity_type, reference_id, metadata)
-- |     values (p_collaboration_id, v_caller_id, v_activity_type, p_collaboration_id, jsonb_build_object('previous_status', v_current_status, 'new_status', p_new_status));
-- |
-- |   if v_activity_type = 'collaboration_completed' then
-- |     v_notif_type := 'collaboration_completed';
-- |     v_notif_title := 'Collaboration completed';
-- |     v_notif_message := 'Collaboration "' || v_collab_title || '" was completed';
-- |   elsif v_activity_type = 'collaboration_reopened' then
-- |     v_notif_type := 'collaboration_reopened';
-- |     v_notif_title := 'Collaboration reopened';
-- |     v_notif_message := 'Collaboration "' || v_collab_title || '" was reopened';
-- |   elsif v_activity_type = 'collaboration_archived' then
-- |     v_notif_type := 'collaboration_archived';
-- |     v_notif_title := 'Collaboration archived';
-- |     v_notif_message := 'Collaboration "' || v_collab_title || '" was archived';
-- |   end if;
-- |
-- |   if v_notif_type is not null then
-- |     insert into public.notifications (user_id, from_user_id, type, title, message, target_type, target_id)
-- |       select cp.user_id, v_caller_id, v_notif_type, v_notif_title, v_notif_message, 'collaboration', p_collaboration_id
-- |       from public.collaboration_participants cp
-- |       where cp.collaboration_id = p_collaboration_id
-- |         and cp.user_id <> v_caller_id;
-- |   end if;
-- | end;
-- | $function$
-- |
-- <<< END function public.change_collaboration_status(p_collaboration_id uuid, p_new_status text)
--
-- ===== public.close_own_wanted_post(p_post_id uuid) =====
--   returns            : void
--   role in the app    : frontend RPC
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,authenticated=X/postgres,service_role=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 604 bytes, md5 e99ba64f7109986005ddd0d07157bf70
--   RAISE sites        : 2  -> P0001 no_active_stagerz_identity; P0008 post_not_found_or_not_open_or_not_owned
-- >>> BEGIN function public.close_own_wanted_post(p_post_id uuid)
-- >>> sha256 30143c7184b1f7a34d03022ad8034190f69c021afc1eb2eed8ca25e7abeda916
-- | CREATE OR REPLACE FUNCTION public.close_own_wanted_post(p_post_id uuid)
-- |  RETURNS void
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_user_id uuid := public.current_active_stagerz_user_id();
-- | begin
-- |   if v_user_id is null then
-- |     raise exception 'no_active_stagerz_identity' using errcode = 'P0001';
-- |   end if;
-- |   update public.wanted_posts
-- |     set status = 'closed'
-- |     where id = p_post_id and user_id = v_user_id and status = 'open';
-- |   if not found then
-- |     raise exception 'post_not_found_or_not_open_or_not_owned' using errcode = 'P0008';
-- |   end if;
-- | end;
-- | $function$
-- |
-- <<< END function public.close_own_wanted_post(p_post_id uuid)
--
-- ===== public.complete_collaboration_task(p_task_id uuid) =====
--   returns            : void
--   role in the app    : frontend RPC
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres,authenticated=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 2005 bytes, md5 d02a9905006fe19d9b2167bc70272b22
--   RAISE sites        : 5  -> P0001 no_active_stagerz_identity; P0020 task_not_found; P0014 collaboration_not_active; P0015 not_a_participant; P0021 task_not_found_or_not_todo
-- >>> BEGIN function public.complete_collaboration_task(p_task_id uuid)
-- >>> sha256 d5aa3ae525e70acb20afb20b5b1fce4a88013207b6352b7239b152d3e88cbdd6
-- | CREATE OR REPLACE FUNCTION public.complete_collaboration_task(p_task_id uuid)
-- |  RETURNS void
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_caller_id uuid := public.current_active_stagerz_user_id();
-- |   v_collaboration_id uuid;
-- |   v_collab_status text;
-- |   v_title text;
-- |   v_collab_title text;
-- | begin
-- |   if v_caller_id is null then
-- |     raise exception 'no_active_stagerz_identity' using errcode = 'P0001';
-- |   end if;
-- |
-- |   select collaboration_id into v_collaboration_id
-- |     from public.collaboration_tasks where id = p_task_id;
-- |
-- |   if v_collaboration_id is null then
-- |     raise exception 'task_not_found' using errcode = 'P0020';
-- |   end if;
-- |
-- |   select status, title into v_collab_status, v_collab_title from public.collaborations where id = v_collaboration_id;
-- |   if v_collab_status <> 'active' then
-- |     raise exception 'collaboration_not_active' using errcode = 'P0014';
-- |   end if;
-- |
-- |   if not public.is_collaboration_participant(v_collaboration_id) then
-- |     raise exception 'not_a_participant' using errcode = 'P0015';
-- |   end if;
-- |
-- |   update public.collaboration_tasks
-- |     set status = 'done', completed_at = now()
-- |     where id = p_task_id and status = 'todo'
-- |     returning title into v_title;
-- |
-- |   if not found then
-- |     raise exception 'task_not_found_or_not_todo' using errcode = 'P0021';
-- |   end if;
-- |
-- |   insert into public.collaboration_activity (collaboration_id, actor_user_id, activity_type, reference_id, metadata)
-- |     values (v_collaboration_id, v_caller_id, 'task_completed', p_task_id, jsonb_build_object('title', v_title));
-- |
-- |   insert into public.notifications (user_id, from_user_id, type, title, message, target_type, target_id)
-- |     select cp.user_id, v_caller_id, 'collaboration_task_completed', 'Task completed',
-- |            'Task "' || v_title || '" completed in "' || v_collab_title || '"', 'collaboration', v_collaboration_id
-- |     from public.collaboration_participants cp
-- |     where cp.collaboration_id = v_collaboration_id
-- |       and cp.user_id <> v_caller_id;
-- | end;
-- | $function$
-- |
-- <<< END function public.complete_collaboration_task(p_task_id uuid)
--
-- ===== public.create_collaboration_credit(p_collaboration_id uuid, p_user_id uuid, p_role text) =====
--   returns            : TABLE(credit_id uuid)
--   role in the app    : frontend RPC
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,authenticated=X/postgres,service_role=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 2397 bytes, md5 a23b083b9c58931f236282e8f05e5f0b
--   RAISE sites        : 7  -> P0001 no_active_stagerz_identity; P0016 collaboration_not_found; P0014 collaboration_not_active; P0015 not_a_participant; P0052 credited_user_not_a_participant; P0049 role_empty; P0053 duplicate_credit
-- >>> BEGIN function public.create_collaboration_credit(p_collaboration_id uuid, p_user_id uuid, p_role text)
-- >>> sha256 f06bf5a3de37878b04dc19f61226f0b0579f701b5eaba9d4ee951be4cd2f2633
-- | CREATE OR REPLACE FUNCTION public.create_collaboration_credit(p_collaboration_id uuid, p_user_id uuid, p_role text)
-- |  RETURNS TABLE(credit_id uuid)
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_caller_id uuid := public.current_active_stagerz_user_id();
-- |   v_collab_status text;
-- |   v_trimmed_role text;
-- |   v_next_order int;
-- |   v_new_credit_id uuid;
-- | begin
-- |   if v_caller_id is null then
-- |     raise exception 'no_active_stagerz_identity' using errcode = 'P0001';
-- |   end if;
-- |
-- |   select status into v_collab_status from public.collaborations where id = p_collaboration_id;
-- |   if v_collab_status is null then
-- |     raise exception 'collaboration_not_found' using errcode = 'P0016';
-- |   end if;
-- |   if v_collab_status <> 'active' then
-- |     raise exception 'collaboration_not_active' using errcode = 'P0014';
-- |   end if;
-- |
-- |   if not public.is_collaboration_participant(p_collaboration_id) then
-- |     raise exception 'not_a_participant' using errcode = 'P0015';
-- |   end if;
-- |
-- |   if not exists (
-- |     select 1 from public.collaboration_participants
-- |     where collaboration_id = p_collaboration_id and user_id = p_user_id
-- |   ) then
-- |     raise exception 'credited_user_not_a_participant' using errcode = 'P0052';
-- |   end if;
-- |
-- |   v_trimmed_role := trim(p_role);
-- |   if length(v_trimmed_role) = 0 then
-- |     raise exception 'role_empty' using errcode = 'P0049';
-- |   end if;
-- |
-- |   -- Duplicate prevention: one active credit per participant per
-- |   -- collaboration. Since credit deletion is a genuine hard delete
-- |   -- (Phase 16.1, Task model, no soft-delete), a NEW credit for the
-- |   -- same participant is correctly allowed again once any prior one
-- |   -- has actually been removed -- this check only ever sees currently
-- |   -- existing rows, never historical ones.
-- |   if exists (
-- |     select 1 from public.collaboration_credits
-- |     where collaboration_id = p_collaboration_id and user_id = p_user_id
-- |   ) then
-- |     raise exception 'duplicate_credit' using errcode = 'P0053';
-- |   end if;
-- |
-- |   select coalesce(max(credit_order), -1) + 1 into v_next_order
-- |     from public.collaboration_credits where collaboration_id = p_collaboration_id;
-- |
-- |   insert into public.collaboration_credits (collaboration_id, user_id, credit_role, credit_order, created_by)
-- |     values (p_collaboration_id, p_user_id, v_trimmed_role, v_next_order, v_caller_id)
-- |     returning id into v_new_credit_id;
-- |
-- |   return query select v_new_credit_id;
-- | end;
-- | $function$
-- |
-- <<< END function public.create_collaboration_credit(p_collaboration_id uuid, p_user_id uuid, p_role text)
--
-- ===== public.create_collaboration_message(p_collaboration_id uuid, p_body text) =====
--   returns            : TABLE(message_id uuid)
--   role in the app    : frontend RPC
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,authenticated=X/postgres,service_role=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 1795 bytes, md5 31feeb2c1015fca95d3a84b0d6f11a46
--   RAISE sites        : 6  -> P0001 no_active_stagerz_identity; P0016 collaboration_not_found; P0014 collaboration_not_active; P0015 not_a_participant; P0056 body_empty; P0057 body_too_long
-- >>> BEGIN function public.create_collaboration_message(p_collaboration_id uuid, p_body text)
-- >>> sha256 15b22ff0ccd3b25e3d35579b3e6079334da0a3cde2bfb616f1e83847bafea365
-- | CREATE OR REPLACE FUNCTION public.create_collaboration_message(p_collaboration_id uuid, p_body text)
-- |  RETURNS TABLE(message_id uuid)
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_caller_id uuid := public.current_active_stagerz_user_id();
-- |   v_collab_status text;
-- |   v_trimmed_body text;
-- |   v_new_message_id uuid;
-- | begin
-- |   if v_caller_id is null then
-- |     raise exception 'no_active_stagerz_identity' using errcode = 'P0001';
-- |   end if;
-- |
-- |   select status into v_collab_status from public.collaborations where id = p_collaboration_id;
-- |   if v_collab_status is null then
-- |     raise exception 'collaboration_not_found' using errcode = 'P0016';
-- |   end if;
-- |   if v_collab_status <> 'active' then
-- |     raise exception 'collaboration_not_active' using errcode = 'P0014';
-- |   end if;
-- |
-- |   if not public.is_collaboration_participant(p_collaboration_id) then
-- |     raise exception 'not_a_participant' using errcode = 'P0015';
-- |   end if;
-- |
-- |   v_trimmed_body := trim(p_body);
-- |   if length(v_trimmed_body) = 0 then
-- |     raise exception 'body_empty' using errcode = 'P0056';
-- |   end if;
-- |   if length(v_trimmed_body) > 5000 then
-- |     raise exception 'body_too_long' using errcode = 'P0057';
-- |   end if;
-- |
-- |   -- sender_id is derived here, server-side, from the resolved
-- |   -- caller identity -- never accepted as a parameter, never
-- |   -- trusted from the client. The existing AFTER INSERT trigger
-- |   -- (log_collaboration_message_activity) fires exactly as it
-- |   -- already does for a direct insert -- no parallel activity or
-- |   -- notification logic is introduced here.
-- |   insert into public.collaboration_messages (collaboration_id, sender_id, body)
-- |     values (p_collaboration_id, v_caller_id, v_trimmed_body)
-- |     returning id into v_new_message_id;
-- |
-- |   return query select v_new_message_id;
-- | end;
-- | $function$
-- |
-- <<< END function public.create_collaboration_message(p_collaboration_id uuid, p_body text)
--
-- ===== public.create_collaboration_task(p_collaboration_id uuid, p_title text, p_assignee_id uuid) =====
--   returns            : TABLE(task_id uuid)
--   role in the app    : frontend RPC
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres,authenticated=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 2515 bytes, md5 7107df6b9b764caff574397663f3fb91
--   RAISE sites        : 7  -> P0001 no_active_stagerz_identity; P0016 collaboration_not_found; P0014 collaboration_not_active; P0015 not_a_participant; P0017 title_empty; P0018 title_too_long; P0019 assignee_not_a_participant
-- >>> BEGIN function public.create_collaboration_task(p_collaboration_id uuid, p_title text, p_assignee_id uuid)
-- >>> sha256 7d6e6b83132c0e40803f9640fc83ded2f0baf24894dc1aa6cff4fb825d81926b
-- | CREATE OR REPLACE FUNCTION public.create_collaboration_task(p_collaboration_id uuid, p_title text, p_assignee_id uuid DEFAULT NULL::uuid)
-- |  RETURNS TABLE(task_id uuid)
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_caller_id uuid := public.current_active_stagerz_user_id();
-- |   v_collab_status text;
-- |   v_trimmed_title text;
-- |   v_new_task_id uuid;
-- |   v_collab_title text;
-- | begin
-- |   if v_caller_id is null then
-- |     raise exception 'no_active_stagerz_identity' using errcode = 'P0001';
-- |   end if;
-- |
-- |   select status, title into v_collab_status, v_collab_title from public.collaborations where id = p_collaboration_id;
-- |   if v_collab_status is null then
-- |     raise exception 'collaboration_not_found' using errcode = 'P0016';
-- |   end if;
-- |   if v_collab_status <> 'active' then
-- |     raise exception 'collaboration_not_active' using errcode = 'P0014';
-- |   end if;
-- |
-- |   if not public.is_collaboration_participant(p_collaboration_id) then
-- |     raise exception 'not_a_participant' using errcode = 'P0015';
-- |   end if;
-- |
-- |   v_trimmed_title := trim(p_title);
-- |   if length(v_trimmed_title) = 0 then
-- |     raise exception 'title_empty' using errcode = 'P0017';
-- |   end if;
-- |   if length(v_trimmed_title) > 300 then
-- |     raise exception 'title_too_long' using errcode = 'P0018';
-- |   end if;
-- |
-- |   if p_assignee_id is not null then
-- |     if not exists (
-- |       select 1 from public.collaboration_participants
-- |       where collaboration_id = p_collaboration_id and user_id = p_assignee_id
-- |     ) then
-- |       raise exception 'assignee_not_a_participant' using errcode = 'P0019';
-- |     end if;
-- |   end if;
-- |
-- |   insert into public.collaboration_tasks (collaboration_id, creator_id, assignee_id, title)
-- |     values (p_collaboration_id, v_caller_id, p_assignee_id, v_trimmed_title)
-- |     returning id into v_new_task_id;
-- |
-- |   insert into public.collaboration_activity (collaboration_id, actor_user_id, activity_type, reference_id, metadata)
-- |     values (p_collaboration_id, v_caller_id, 'task_created', v_new_task_id, jsonb_build_object('title', v_trimmed_title));
-- |
-- |   insert into public.notifications (user_id, from_user_id, type, title, message, target_type, target_id)
-- |     select cp.user_id, v_caller_id, 'collaboration_task_created', 'New task',
-- |            'New task "' || v_trimmed_title || '" in "' || v_collab_title || '"', 'collaboration', p_collaboration_id
-- |     from public.collaboration_participants cp
-- |     where cp.collaboration_id = p_collaboration_id
-- |       and cp.user_id <> v_caller_id;
-- |
-- |   return query select v_new_task_id;
-- | end;
-- | $function$
-- |
-- <<< END function public.create_collaboration_task(p_collaboration_id uuid, p_title text, p_assignee_id uuid)
--
-- ===== public.create_wanted_application(p_wanted_post_id uuid) =====
--   returns            : TABLE(application_id uuid)
--   role in the app    : frontend RPC
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres,authenticated=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 1594 bytes, md5 d483dfaff6c38abab39e19382da0fd61
--   RAISE sites        : 4  -> P0001 no_active_stagerz_identity; P0011 wanted_post_not_found; P0012 wanted_post_not_open; P0013 cannot_apply_to_own_wanted
-- >>> BEGIN function public.create_wanted_application(p_wanted_post_id uuid)
-- >>> sha256 c388a0a40c4ca3d4fae818b7e0a714c0aeeab382849541f60599a246fb3e46d1
-- | CREATE OR REPLACE FUNCTION public.create_wanted_application(p_wanted_post_id uuid)
-- |  RETURNS TABLE(application_id uuid)
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_applicant_id uuid := public.current_active_stagerz_user_id();
-- |   v_owner_id uuid;
-- |   v_status text;
-- |   v_title text;
-- |   v_new_application_id uuid;
-- | begin
-- |   if v_applicant_id is null then
-- |     raise exception 'no_active_stagerz_identity' using errcode = 'P0001';
-- |   end if;
-- |
-- |   select user_id, status, title into v_owner_id, v_status, v_title
-- |     from public.wanted_posts
-- |     where id = p_wanted_post_id;
-- |
-- |   if v_owner_id is null then
-- |     raise exception 'wanted_post_not_found' using errcode = 'P0011';
-- |   end if;
-- |
-- |   if v_status <> 'open' then
-- |     raise exception 'wanted_post_not_open' using errcode = 'P0012';
-- |   end if;
-- |
-- |   if v_owner_id = v_applicant_id then
-- |     raise exception 'cannot_apply_to_own_wanted' using errcode = 'P0013';
-- |   end if;
-- |
-- |   insert into public.wanted_applications (wanted_post_id, applicant_id)
-- |     values (p_wanted_post_id, v_applicant_id)
-- |     returning id into v_new_application_id;
-- |
-- |   insert into public.notifications (user_id, from_user_id, type, title, message, wanted_post_id, wanted_application_id, read, target_type, target_id)
-- |     values (
-- |       v_owner_id,
-- |       v_applicant_id,
-- |       'wanted.application.created',
-- |       'New application',
-- |       'New application for "' || v_title || '"',
-- |       p_wanted_post_id,
-- |       v_new_application_id,
-- |       false,
-- |       'wanted_post',
-- |       p_wanted_post_id
-- |     );
-- |
-- |   return query select v_new_application_id;
-- | end;
-- | $function$
-- |
-- <<< END function public.create_wanted_application(p_wanted_post_id uuid)
--
-- ===== public.current_active_stagerz_user_id() =====
--   returns            : uuid
--   role in the app    : helper
--   language           : sql
--   volatility         : STABLE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres,authenticated=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 367 bytes, md5 b561e324f76798cfc8759cf3aead88fb
--   RAISE sites        : 0
-- >>> BEGIN function public.current_active_stagerz_user_id()
-- >>> sha256 90dad6cd09a08c5fc0a5a4427b3625ffe380689782bb4532c2774a0f5b702726
-- | CREATE OR REPLACE FUNCTION public.current_active_stagerz_user_id()
-- |  RETURNS uuid
-- |  LANGUAGE sql
-- |  STABLE SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- |   select u.id
-- |   from public.user_auth_accounts uaa
-- |   join public.users u on u.id = uaa.public_user_id
-- |   where uaa.auth_user_id = auth.uid()
-- |     and u.blocked = false
-- |     and u.anonymized_at is null;
-- | $function$
-- |
-- <<< END function public.current_active_stagerz_user_id()
--
-- ===== public.current_stagerz_user_id() =====
--   returns            : uuid
--   role in the app    : helper
--   language           : sql
--   volatility         : STABLE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres,authenticated=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : Returns null when no authenticated or mapped identity exists for a role permitted to execute it; anon has no direct EXECUTE privilege.
--   definition         : 249 bytes, md5 dbc3ab83dd45f87bc1d040a4e127f0ef
--   RAISE sites        : 0
-- >>> BEGIN function public.current_stagerz_user_id()
-- >>> sha256 18146d74acbab9bc1e67e1cffa9028b3feef9e617b4911d118c84ef7e3e75ba1
-- | CREATE OR REPLACE FUNCTION public.current_stagerz_user_id()
-- |  RETURNS uuid
-- |  LANGUAGE sql
-- |  STABLE SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- |   select public_user_id from public.user_auth_accounts where auth_user_id = auth.uid();
-- | $function$
-- |
-- <<< END function public.current_stagerz_user_id()
--
-- ===== public.delete_collaboration_asset(p_asset_id uuid) =====
--   returns            : void
--   role in the app    : frontend RPC
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres,authenticated=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 2066 bytes, md5 0e43b560f1e9f5f55a6c1458b6329f24
--   RAISE sites        : 6  -> P0001 no_active_stagerz_identity; P0044 asset_not_found; P0045 asset_pending_deletion; P0014 collaboration_not_active; P0046 only_uploader_or_owner_may_delete; P0043 delete_failed
-- >>> BEGIN function public.delete_collaboration_asset(p_asset_id uuid)
-- >>> sha256 760b735f77b2943451548a6fbec032ade1bb0999329f2249a7ff15ed85fae53b
-- | CREATE OR REPLACE FUNCTION public.delete_collaboration_asset(p_asset_id uuid)
-- |  RETURNS void
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_caller_id uuid := public.current_active_stagerz_user_id();
-- |   v_collaboration_id uuid;
-- |   v_collab_status text;
-- |   v_uploaded_by uuid;
-- |   v_file_name text;
-- |   v_storage_path text;
-- |   v_deleted_at timestamptz;
-- |   v_rows_changed int;
-- | begin
-- |   if v_caller_id is null then
-- |     raise exception 'no_active_stagerz_identity' using errcode = 'P0001';
-- |   end if;
-- |
-- |   select collaboration_id, uploaded_by, file_name, storage_path, deleted_at
-- |     into v_collaboration_id, v_uploaded_by, v_file_name, v_storage_path, v_deleted_at
-- |     from public.collaboration_assets where id = p_asset_id;
-- |   if v_collaboration_id is null then
-- |     raise exception 'asset_not_found' using errcode = 'P0044';
-- |   end if;
-- |   if v_deleted_at is not null then
-- |     raise exception 'asset_pending_deletion' using errcode = 'P0045';
-- |   end if;
-- |
-- |   select status into v_collab_status from public.collaborations where id = v_collaboration_id;
-- |   if v_collab_status <> 'active' then
-- |     raise exception 'collaboration_not_active' using errcode = 'P0014';
-- |   end if;
-- |
-- |   if v_caller_id <> v_uploaded_by and not public.is_collaboration_owner(v_collaboration_id) then
-- |     raise exception 'only_uploader_or_owner_may_delete' using errcode = 'P0046';
-- |   end if;
-- |
-- |   update public.collaboration_assets
-- |     set deleted_at = now()
-- |     where id = p_asset_id and deleted_at is null;
-- |   get diagnostics v_rows_changed = row_count;
-- |   if v_rows_changed <> 1 then
-- |     raise exception 'delete_failed' using errcode = 'P0043';
-- |   end if;
-- |
-- |   insert into public.pending_asset_deletions (asset_id, collaboration_id, file_name, storage_path)
-- |     values (p_asset_id, v_collaboration_id, v_file_name, v_storage_path);
-- |
-- |   insert into public.collaboration_activity (collaboration_id, actor_user_id, activity_type, reference_id, metadata)
-- |     values (v_collaboration_id, v_caller_id, 'asset_deleted', p_asset_id, jsonb_build_object('file_name', v_file_name));
-- | end;
-- | $function$
-- |
-- <<< END function public.delete_collaboration_asset(p_asset_id uuid)
--
-- ===== public.delete_collaboration_credit(p_credit_id uuid) =====
--   returns            : void
--   role in the app    : frontend RPC
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres,authenticated=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 1662 bytes, md5 bdc86658406ba1d713387b804f52f4af
--   RAISE sites        : 5  -> P0001 no_active_stagerz_identity; P0047 credit_not_found; P0014 collaboration_not_active; P0050 only_owner_may_delete_credit; P0051 delete_failed
-- >>> BEGIN function public.delete_collaboration_credit(p_credit_id uuid)
-- >>> sha256 68b70e3e66ebbbddaf86e89013cba82210a67e6286932704ce983ccc947837a1
-- | CREATE OR REPLACE FUNCTION public.delete_collaboration_credit(p_credit_id uuid)
-- |  RETURNS void
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_caller_id uuid := public.current_active_stagerz_user_id();
-- |   v_collaboration_id uuid;
-- |   v_collab_status text;
-- |   v_participant_id uuid;
-- |   v_role text;
-- |   v_rows_changed int;
-- | begin
-- |   if v_caller_id is null then
-- |     raise exception 'no_active_stagerz_identity' using errcode = 'P0001';
-- |   end if;
-- |
-- |   select collaboration_id, user_id, credit_role
-- |     into v_collaboration_id, v_participant_id, v_role
-- |     from public.collaboration_credits where id = p_credit_id;
-- |   if v_collaboration_id is null then
-- |     raise exception 'credit_not_found' using errcode = 'P0047';
-- |   end if;
-- |
-- |   select status into v_collab_status from public.collaborations where id = v_collaboration_id;
-- |   if v_collab_status <> 'active' then
-- |     raise exception 'collaboration_not_active' using errcode = 'P0014';
-- |   end if;
-- |
-- |   if not public.is_collaboration_owner(v_collaboration_id) then
-- |     raise exception 'only_owner_may_delete_credit' using errcode = 'P0050';
-- |   end if;
-- |
-- |   insert into public.collaboration_activity (collaboration_id, actor_user_id, activity_type, reference_id, metadata)
-- |     values (v_collaboration_id, v_caller_id, 'credit_deleted', p_credit_id, jsonb_build_object(
-- |       'credit_id', p_credit_id,
-- |       'participant_id', v_participant_id,
-- |       'role', v_role
-- |     ));
-- |
-- |   delete from public.collaboration_credits where id = p_credit_id;
-- |   get diagnostics v_rows_changed = row_count;
-- |   if v_rows_changed <> 1 then
-- |     raise exception 'delete_failed' using errcode = 'P0051';
-- |   end if;
-- | end;
-- | $function$
-- |
-- <<< END function public.delete_collaboration_credit(p_credit_id uuid)
--
-- ===== public.delete_collaboration_message(p_message_id uuid) =====
--   returns            : void
--   role in the app    : frontend RPC
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,authenticated=X/postgres,service_role=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 1924 bytes, md5 1564c4fd098df4bef5a32a1d400a82ec
--   RAISE sites        : 6  -> P0001 no_active_stagerz_identity; P0054 message_not_found; P0016 collaboration_not_found; P0014 collaboration_not_active; P0058 only_author_or_owner_may_delete_message; P0059 delete_failed
-- >>> BEGIN function public.delete_collaboration_message(p_message_id uuid)
-- >>> sha256 076d9348e7d9f810b343a323620b9c2c931b4563f849e9b2cea6d4df719dc5ba
-- | CREATE OR REPLACE FUNCTION public.delete_collaboration_message(p_message_id uuid)
-- |  RETURNS void
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_caller_id uuid := public.current_active_stagerz_user_id();
-- |   v_collaboration_id uuid;
-- |   v_sender_id uuid;
-- |   v_collab_status text;
-- |   v_rows_changed int;
-- | begin
-- |   if v_caller_id is null then
-- |     raise exception 'no_active_stagerz_identity' using errcode = 'P0001';
-- |   end if;
-- |
-- |   select collaboration_id, sender_id into v_collaboration_id, v_sender_id
-- |     from public.collaboration_messages where id = p_message_id;
-- |   if v_collaboration_id is null then
-- |     raise exception 'message_not_found' using errcode = 'P0054';
-- |   end if;
-- |
-- |   select status into v_collab_status from public.collaborations where id = v_collaboration_id;
-- |   if v_collab_status is null then
-- |     raise exception 'collaboration_not_found' using errcode = 'P0016';
-- |   end if;
-- |   if v_collab_status <> 'active' then
-- |     raise exception 'collaboration_not_active' using errcode = 'P0014';
-- |   end if;
-- |
-- |   -- Author or collaboration owner -- exactly the Task deletion model.
-- |   if v_caller_id <> v_sender_id and not public.is_collaboration_owner(v_collaboration_id) then
-- |     raise exception 'only_author_or_owner_may_delete_message' using errcode = 'P0058';
-- |   end if;
-- |
-- |   -- Hard delete: no soft-delete precedent exists for this table (no
-- |   -- deleted_at column, no Storage coordination dependency) -- the
-- |   -- Task model applies, not the Asset model.
-- |   insert into public.collaboration_activity (collaboration_id, actor_user_id, activity_type, reference_id, metadata)
-- |     values (v_collaboration_id, v_caller_id, 'message_deleted', p_message_id, '{}'::jsonb);
-- |
-- |   delete from public.collaboration_messages where id = p_message_id;
-- |   get diagnostics v_rows_changed = row_count;
-- |   if v_rows_changed <> 1 then
-- |     raise exception 'delete_failed' using errcode = 'P0059';
-- |   end if;
-- | end;
-- | $function$
-- |
-- <<< END function public.delete_collaboration_message(p_message_id uuid)
--
-- ===== public.delete_collaboration_task(p_task_id uuid) =====
--   returns            : void
--   role in the app    : frontend RPC
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres,authenticated=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 1583 bytes, md5 aa19739f30de46b093c3206d5f5b3c50
--   RAISE sites        : 5  -> P0001 no_active_stagerz_identity; P0020 task_not_found; P0014 collaboration_not_active; P0042 only_creator_or_owner_may_delete; P0043 delete_failed
-- >>> BEGIN function public.delete_collaboration_task(p_task_id uuid)
-- >>> sha256 b8b793d5123c31ffb3ce2745511922ce5bc844af12cdbcf7670c9c82893adfc4
-- | CREATE OR REPLACE FUNCTION public.delete_collaboration_task(p_task_id uuid)
-- |  RETURNS void
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_caller_id uuid := public.current_active_stagerz_user_id();
-- |   v_collaboration_id uuid;
-- |   v_collab_status text;
-- |   v_creator_id uuid;
-- |   v_title text;
-- |   v_rows_changed int;
-- | begin
-- |   if v_caller_id is null then
-- |     raise exception 'no_active_stagerz_identity' using errcode = 'P0001';
-- |   end if;
-- |
-- |   select collaboration_id, creator_id, title into v_collaboration_id, v_creator_id, v_title
-- |     from public.collaboration_tasks where id = p_task_id;
-- |   if v_collaboration_id is null then
-- |     raise exception 'task_not_found' using errcode = 'P0020';
-- |   end if;
-- |
-- |   select status into v_collab_status from public.collaborations where id = v_collaboration_id;
-- |   if v_collab_status <> 'active' then
-- |     raise exception 'collaboration_not_active' using errcode = 'P0014';
-- |   end if;
-- |
-- |   if v_caller_id <> v_creator_id and not public.is_collaboration_owner(v_collaboration_id) then
-- |     raise exception 'only_creator_or_owner_may_delete' using errcode = 'P0042';
-- |   end if;
-- |
-- |   delete from public.collaboration_tasks where id = p_task_id;
-- |   get diagnostics v_rows_changed = row_count;
-- |   if v_rows_changed <> 1 then
-- |     raise exception 'delete_failed' using errcode = 'P0043';
-- |   end if;
-- |
-- |   insert into public.collaboration_activity (collaboration_id, actor_user_id, activity_type, reference_id, metadata)
-- |     values (v_collaboration_id, v_caller_id, 'task_deleted', p_task_id, jsonb_build_object('title', v_title));
-- | end;
-- | $function$
-- |
-- <<< END function public.delete_collaboration_task(p_task_id uuid)
--
-- ===== public.edit_collaboration_asset(p_asset_id uuid, p_title text, p_description text) =====
--   returns            : void
--   role in the app    : frontend RPC
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres,authenticated=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 2106 bytes, md5 26f503f4526d9acc069763b7a6f7d5f6
--   RAISE sites        : 5  -> P0001 no_active_stagerz_identity; P0044 asset_not_found; P0045 asset_pending_deletion; P0014 collaboration_not_active; P0015 not_a_participant
-- >>> BEGIN function public.edit_collaboration_asset(p_asset_id uuid, p_title text, p_description text)
-- >>> sha256 b85a2758c037599ddf9282ea90e58cb0592520e526522aebfff88051eabf52bf
-- | CREATE OR REPLACE FUNCTION public.edit_collaboration_asset(p_asset_id uuid, p_title text, p_description text)
-- |  RETURNS void
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_caller_id uuid := public.current_active_stagerz_user_id();
-- |   v_collaboration_id uuid;
-- |   v_collab_status text;
-- |   v_deleted_at timestamptz;
-- |   v_file_name text;
-- |   v_previous_title text;
-- |   v_previous_description text;
-- |   v_new_title text;
-- |   v_new_description text;
-- | begin
-- |   if v_caller_id is null then
-- |     raise exception 'no_active_stagerz_identity' using errcode = 'P0001';
-- |   end if;
-- |
-- |   select collaboration_id, deleted_at, file_name, title, description
-- |     into v_collaboration_id, v_deleted_at, v_file_name, v_previous_title, v_previous_description
-- |     from public.collaboration_assets where id = p_asset_id;
-- |   if v_collaboration_id is null then
-- |     raise exception 'asset_not_found' using errcode = 'P0044';
-- |   end if;
-- |   if v_deleted_at is not null then
-- |     raise exception 'asset_pending_deletion' using errcode = 'P0045';
-- |   end if;
-- |
-- |   select status into v_collab_status from public.collaborations where id = v_collaboration_id;
-- |   if v_collab_status <> 'active' then
-- |     raise exception 'collaboration_not_active' using errcode = 'P0014';
-- |   end if;
-- |
-- |   if not public.is_collaboration_participant(v_collaboration_id) then
-- |     raise exception 'not_a_participant' using errcode = 'P0015';
-- |   end if;
-- |
-- |   v_new_title := nullif(trim(p_title), '');
-- |   v_new_description := nullif(trim(p_description), '');
-- |
-- |   update public.collaboration_assets
-- |     set title = v_new_title,
-- |         description = v_new_description
-- |     where id = p_asset_id;
-- |
-- |   insert into public.collaboration_activity (collaboration_id, actor_user_id, activity_type, reference_id, metadata)
-- |     values (v_collaboration_id, v_caller_id, 'asset_edited', p_asset_id, jsonb_build_object(
-- |       'asset_id', p_asset_id,
-- |       'file_name', v_file_name,
-- |       'previous_title', v_previous_title,
-- |       'new_title', v_new_title,
-- |       'previous_description', v_previous_description,
-- |       'new_description', v_new_description
-- |     ));
-- | end;
-- | $function$
-- |
-- <<< END function public.edit_collaboration_asset(p_asset_id uuid, p_title text, p_description text)
--
-- ===== public.edit_collaboration_credit(p_credit_id uuid, p_role text) =====
--   returns            : void
--   role in the app    : frontend RPC
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres,authenticated=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 1750 bytes, md5 5e70bcd7c3fc4fc6754455792cb4a499
--   RAISE sites        : 5  -> P0001 no_active_stagerz_identity; P0047 credit_not_found; P0014 collaboration_not_active; P0048 only_owner_may_edit_credit; P0049 role_empty
-- >>> BEGIN function public.edit_collaboration_credit(p_credit_id uuid, p_role text)
-- >>> sha256 48ce55b7e4cc3f0d807eac48e1a45ba1f8d3dcb2df4f03040da3a50f546ad7b8
-- | CREATE OR REPLACE FUNCTION public.edit_collaboration_credit(p_credit_id uuid, p_role text)
-- |  RETURNS void
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_caller_id uuid := public.current_active_stagerz_user_id();
-- |   v_collaboration_id uuid;
-- |   v_collab_status text;
-- |   v_participant_id uuid;
-- |   v_previous_role text;
-- |   v_new_role text;
-- | begin
-- |   if v_caller_id is null then
-- |     raise exception 'no_active_stagerz_identity' using errcode = 'P0001';
-- |   end if;
-- |
-- |   select collaboration_id, user_id, credit_role
-- |     into v_collaboration_id, v_participant_id, v_previous_role
-- |     from public.collaboration_credits where id = p_credit_id;
-- |   if v_collaboration_id is null then
-- |     raise exception 'credit_not_found' using errcode = 'P0047';
-- |   end if;
-- |
-- |   select status into v_collab_status from public.collaborations where id = v_collaboration_id;
-- |   if v_collab_status <> 'active' then
-- |     raise exception 'collaboration_not_active' using errcode = 'P0014';
-- |   end if;
-- |
-- |   if not public.is_collaboration_owner(v_collaboration_id) then
-- |     raise exception 'only_owner_may_edit_credit' using errcode = 'P0048';
-- |   end if;
-- |
-- |   v_new_role := trim(p_role);
-- |   if length(v_new_role) = 0 then
-- |     raise exception 'role_empty' using errcode = 'P0049';
-- |   end if;
-- |
-- |   update public.collaboration_credits
-- |     set credit_role = v_new_role
-- |     where id = p_credit_id;
-- |
-- |   insert into public.collaboration_activity (collaboration_id, actor_user_id, activity_type, reference_id, metadata)
-- |     values (v_collaboration_id, v_caller_id, 'credit_edited', p_credit_id, jsonb_build_object(
-- |       'credit_id', p_credit_id,
-- |       'participant_id', v_participant_id,
-- |       'previous_role', v_previous_role,
-- |       'new_role', v_new_role
-- |     ));
-- | end;
-- | $function$
-- |
-- <<< END function public.edit_collaboration_credit(p_credit_id uuid, p_role text)
--
-- ===== public.edit_collaboration_message(p_message_id uuid, p_new_body text) =====
--   returns            : void
--   role in the app    : frontend RPC
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,authenticated=X/postgres,service_role=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 2161 bytes, md5 994d36b64b77815f473cb17a08c2bf4a
--   RAISE sites        : 8  -> P0001 no_active_stagerz_identity; P0054 message_not_found; P0016 collaboration_not_found; P0014 collaboration_not_active; P0015 not_a_participant; P0055 only_author_may_edit_message; P0056 body_empty; P0057 body_too_long
-- >>> BEGIN function public.edit_collaboration_message(p_message_id uuid, p_new_body text)
-- >>> sha256 748e09a65f521b2699c28062be999f748ec9dc3e2621e6aa27670faeae420bb7
-- | CREATE OR REPLACE FUNCTION public.edit_collaboration_message(p_message_id uuid, p_new_body text)
-- |  RETURNS void
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_caller_id uuid := public.current_active_stagerz_user_id();
-- |   v_collaboration_id uuid;
-- |   v_sender_id uuid;
-- |   v_collab_status text;
-- |   v_trimmed_body text;
-- | begin
-- |   if v_caller_id is null then
-- |     raise exception 'no_active_stagerz_identity' using errcode = 'P0001';
-- |   end if;
-- |
-- |   select collaboration_id, sender_id into v_collaboration_id, v_sender_id
-- |     from public.collaboration_messages where id = p_message_id;
-- |   if v_collaboration_id is null then
-- |     raise exception 'message_not_found' using errcode = 'P0054';
-- |   end if;
-- |
-- |   select status into v_collab_status from public.collaborations where id = v_collaboration_id;
-- |   if v_collab_status is null then
-- |     raise exception 'collaboration_not_found' using errcode = 'P0016';
-- |   end if;
-- |   if v_collab_status <> 'active' then
-- |     raise exception 'collaboration_not_active' using errcode = 'P0014';
-- |   end if;
-- |
-- |   if not public.is_collaboration_participant(v_collaboration_id) then
-- |     raise exception 'not_a_participant' using errcode = 'P0015';
-- |   end if;
-- |
-- |   -- Author only -- deliberately stricter than Tasks' any-participant
-- |   -- edit model, per explicit instruction for this phase.
-- |   if v_caller_id <> v_sender_id then
-- |     raise exception 'only_author_may_edit_message' using errcode = 'P0055';
-- |   end if;
-- |
-- |   v_trimmed_body := trim(p_new_body);
-- |   if length(v_trimmed_body) = 0 then
-- |     raise exception 'body_empty' using errcode = 'P0056';
-- |   end if;
-- |   if length(v_trimmed_body) > 5000 then
-- |     raise exception 'body_too_long' using errcode = 'P0057';
-- |   end if;
-- |
-- |   update public.collaboration_messages
-- |     set body = v_trimmed_body
-- |     where id = p_message_id;
-- |
-- |   -- No message content in metadata, matching the established rule
-- |   -- that message text is never stored in collaboration_activity.
-- |   insert into public.collaboration_activity (collaboration_id, actor_user_id, activity_type, reference_id, metadata)
-- |     values (v_collaboration_id, v_caller_id, 'message_edited', p_message_id, '{}'::jsonb);
-- | end;
-- | $function$
-- |
-- <<< END function public.edit_collaboration_message(p_message_id uuid, p_new_body text)
--
-- ===== public.edit_collaboration_task(p_task_id uuid, p_new_title text) =====
--   returns            : void
--   role in the app    : frontend RPC
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres,authenticated=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 1773 bytes, md5 0a09684f96bcb47b3a224af7d7a67e87
--   RAISE sites        : 6  -> P0001 no_active_stagerz_identity; P0020 task_not_found; P0014 collaboration_not_active; P0015 not_a_participant; P0017 title_empty; P0018 title_too_long
-- >>> BEGIN function public.edit_collaboration_task(p_task_id uuid, p_new_title text)
-- >>> sha256 bfc02c49ab6cd70b4e0a7c96ffbaa74435ee24822cb02608e7c658c3e82291e4
-- | CREATE OR REPLACE FUNCTION public.edit_collaboration_task(p_task_id uuid, p_new_title text)
-- |  RETURNS void
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_caller_id uuid := public.current_active_stagerz_user_id();
-- |   v_collaboration_id uuid;
-- |   v_collab_status text;
-- |   v_old_title text;
-- |   v_trimmed_title text;
-- | begin
-- |   if v_caller_id is null then
-- |     raise exception 'no_active_stagerz_identity' using errcode = 'P0001';
-- |   end if;
-- |
-- |   select collaboration_id, title into v_collaboration_id, v_old_title
-- |     from public.collaboration_tasks where id = p_task_id;
-- |   if v_collaboration_id is null then
-- |     raise exception 'task_not_found' using errcode = 'P0020';
-- |   end if;
-- |
-- |   select status into v_collab_status from public.collaborations where id = v_collaboration_id;
-- |   if v_collab_status <> 'active' then
-- |     raise exception 'collaboration_not_active' using errcode = 'P0014';
-- |   end if;
-- |
-- |   if not public.is_collaboration_participant(v_collaboration_id) then
-- |     raise exception 'not_a_participant' using errcode = 'P0015';
-- |   end if;
-- |
-- |   v_trimmed_title := trim(p_new_title);
-- |   if length(v_trimmed_title) = 0 then
-- |     raise exception 'title_empty' using errcode = 'P0017';
-- |   end if;
-- |   if length(v_trimmed_title) > 300 then
-- |     raise exception 'title_too_long' using errcode = 'P0018';
-- |   end if;
-- |
-- |   -- Only title changes -- status and assignee are never touched.
-- |   update public.collaboration_tasks
-- |     set title = v_trimmed_title
-- |     where id = p_task_id;
-- |
-- |   insert into public.collaboration_activity (collaboration_id, actor_user_id, activity_type, reference_id, metadata)
-- |     values (v_collaboration_id, v_caller_id, 'task_edited', p_task_id, jsonb_build_object('previous_title', v_old_title, 'new_title', v_trimmed_title));
-- | end;
-- | $function$
-- |
-- <<< END function public.edit_collaboration_task(p_task_id uuid, p_new_title text)
--
-- ===== public.handle_new_auth_user() =====
--   returns            : trigger
--   role in the app    : trigger
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres}
--   EXECUTE            : postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 538 bytes, md5 47c47604d93e67243a38a42c1349a988
--   RAISE sites        : 0
-- >>> BEGIN function public.handle_new_auth_user()
-- >>> sha256 f7fd457bdae8a5951cfcf488d8e4c1b6b02b1200040e154bf960f3601e87d7e1
-- | CREATE OR REPLACE FUNCTION public.handle_new_auth_user()
-- |  RETURNS trigger
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_new_user_id uuid;
-- | begin
-- |   insert into public.users default values returning id into v_new_user_id;
-- |   insert into public.user_auth_accounts (auth_user_id, public_user_id) values (new.id, v_new_user_id);
-- |   insert into public.profiles (user_id, display_name)
-- |     values (v_new_user_id, coalesce(new.raw_user_meta_data->>'display_name', 'New Artist'));
-- |   return new;
-- | end;
-- | $function$
-- |
-- <<< END function public.handle_new_auth_user()
--
-- ===== public.has_completed_onboarding(p_user_id uuid) =====
--   returns            : boolean
--   role in the app    : helper
--   language           : sql
--   volatility         : STABLE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,authenticated=X/postgres,service_role=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 249 bytes, md5 c7c20afe555fcb25226e4a03cea6ff01
--   RAISE sites        : 0
-- >>> BEGIN function public.has_completed_onboarding(p_user_id uuid)
-- >>> sha256 0cd9aa3ce9972aebec663673104770026c78159366ac077dd69ed237bcffdc03
-- | CREATE OR REPLACE FUNCTION public.has_completed_onboarding(p_user_id uuid)
-- |  RETURNS boolean
-- |  LANGUAGE sql
-- |  STABLE SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- |   select username is not null from public.users where id = p_user_id;
-- | $function$
-- |
-- <<< END function public.has_completed_onboarding(p_user_id uuid)
--
-- ===== public.invite_collaboration_participant(p_collaboration_id uuid, p_user_id uuid) =====
--   returns            : void
--   role in the app    : frontend RPC
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres,authenticated=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 2448 bytes, md5 b8fba269f510ee4fd97df64c166901de
--   RAISE sites        : 8  -> P0001 no_active_stagerz_identity; P0016 collaboration_not_found; P0014 collaboration_not_active; P0030 only_owner_may_invite; P0031 cannot_invite_self; P0032 target_user_not_found; P0033 target_user_not_eligible; P0034 already_a_participant
-- >>> BEGIN function public.invite_collaboration_participant(p_collaboration_id uuid, p_user_id uuid)
-- >>> sha256 fe48a649028d3542f774e11a442e27b9024efe9e53a29cc7e24f83037ceeb26b
-- | CREATE OR REPLACE FUNCTION public.invite_collaboration_participant(p_collaboration_id uuid, p_user_id uuid)
-- |  RETURNS void
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_caller_id uuid := public.current_active_stagerz_user_id();
-- |   v_collab_status text;
-- |   v_collab_title text;
-- |   v_target_blocked boolean;
-- |   v_target_anonymized timestamptz;
-- |   v_target_exists boolean;
-- | begin
-- |   if v_caller_id is null then
-- |     raise exception 'no_active_stagerz_identity' using errcode = 'P0001';
-- |   end if;
-- |
-- |   select status, title into v_collab_status, v_collab_title from public.collaborations where id = p_collaboration_id;
-- |   if v_collab_status is null then
-- |     raise exception 'collaboration_not_found' using errcode = 'P0016';
-- |   end if;
-- |   if v_collab_status <> 'active' then
-- |     raise exception 'collaboration_not_active' using errcode = 'P0014';
-- |   end if;
-- |
-- |   if not public.is_collaboration_owner(p_collaboration_id) then
-- |     raise exception 'only_owner_may_invite' using errcode = 'P0030';
-- |   end if;
-- |
-- |   if p_user_id = v_caller_id then
-- |     raise exception 'cannot_invite_self' using errcode = 'P0031';
-- |   end if;
-- |
-- |   select true, blocked, anonymized_at into v_target_exists, v_target_blocked, v_target_anonymized
-- |     from public.users where id = p_user_id;
-- |   if v_target_exists is null then
-- |     raise exception 'target_user_not_found' using errcode = 'P0032';
-- |   end if;
-- |   if v_target_blocked is true or v_target_anonymized is not null then
-- |     raise exception 'target_user_not_eligible' using errcode = 'P0033';
-- |   end if;
-- |
-- |   if exists (
-- |     select 1 from public.collaboration_participants
-- |     where collaboration_id = p_collaboration_id and user_id = p_user_id
-- |   ) then
-- |     raise exception 'already_a_participant' using errcode = 'P0034';
-- |   end if;
-- |
-- |   insert into public.collaboration_participants (collaboration_id, user_id, participant_type)
-- |     values (p_collaboration_id, p_user_id, 'member');
-- |
-- |   insert into public.collaboration_activity (collaboration_id, actor_user_id, activity_type, reference_id, metadata)
-- |     values (p_collaboration_id, v_caller_id, 'participant_invited', p_user_id, '{}'::jsonb);
-- |
-- |   insert into public.notifications (user_id, from_user_id, type, title, message, target_type, target_id)
-- |     values (p_user_id, v_caller_id, 'collaboration_participant_invited', 'Added to a collaboration',
-- |             'You were added to "' || v_collab_title || '"', 'collaboration', p_collaboration_id);
-- | end;
-- | $function$
-- |
-- <<< END function public.invite_collaboration_participant(p_collaboration_id uuid, p_user_id uuid)
--
-- ===== public.is_collaboration_owner(p_collaboration_id uuid) =====
--   returns            : boolean
--   role in the app    : helper
--   language           : sql
--   volatility         : STABLE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,authenticated=X/postgres,service_role=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 406 bytes, md5 8bf82497028d5adc435bb7a2609bbf58
--   RAISE sites        : 0
-- >>> BEGIN function public.is_collaboration_owner(p_collaboration_id uuid)
-- >>> sha256 17eb83da9b7e149051bb13674d3f3054e85f551d2df2dffd3b3366fb4ab672be
-- | CREATE OR REPLACE FUNCTION public.is_collaboration_owner(p_collaboration_id uuid)
-- |  RETURNS boolean
-- |  LANGUAGE sql
-- |  STABLE SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- |   select exists (
-- |     select 1 from public.collaboration_participants
-- |     where collaboration_id = p_collaboration_id
-- |       and user_id = public.current_active_stagerz_user_id()
-- |       and participant_type = 'owner'
-- |   );
-- | $function$
-- |
-- <<< END function public.is_collaboration_owner(p_collaboration_id uuid)
--
-- ===== public.is_collaboration_participant(p_collaboration_id uuid) =====
--   returns            : boolean
--   role in the app    : helper
--   language           : sql
--   volatility         : STABLE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,authenticated=X/postgres,service_role=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 368 bytes, md5 16e8f71893e1a43c77504a558ea82612
--   RAISE sites        : 0
-- >>> BEGIN function public.is_collaboration_participant(p_collaboration_id uuid)
-- >>> sha256 c3804d9c1ca3817e33442cc50f559b0d12f715b0f455d891569c92d72cd5cfb7
-- | CREATE OR REPLACE FUNCTION public.is_collaboration_participant(p_collaboration_id uuid)
-- |  RETURNS boolean
-- |  LANGUAGE sql
-- |  STABLE SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- |   select exists (
-- |     select 1 from public.collaboration_participants
-- |     where collaboration_id = p_collaboration_id
-- |       and user_id = public.current_stagerz_user_id()
-- |   );
-- | $function$
-- |
-- <<< END function public.is_collaboration_participant(p_collaboration_id uuid)
--
-- ===== public.leave_collaboration(p_collaboration_id uuid) =====
--   returns            : void
--   role in the app    : frontend RPC
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres,authenticated=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 1984 bytes, md5 8c08e10c0eff7290d451e0568d2d40ca
--   RAISE sites        : 5  -> P0001 no_active_stagerz_identity; P0016 collaboration_not_found; P0015 not_a_participant; P0040 owner_must_transfer_before_leaving; P0041 leave_failed
-- >>> BEGIN function public.leave_collaboration(p_collaboration_id uuid)
-- >>> sha256 0a04fff28d5444268e4ac6000c986537fa5218eab4b7199523032876750cab7a
-- | CREATE OR REPLACE FUNCTION public.leave_collaboration(p_collaboration_id uuid)
-- |  RETURNS void
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_caller_id uuid := public.current_active_stagerz_user_id();
-- |   v_collab_title text;
-- |   v_caller_type text;
-- |   v_rows_changed int;
-- | begin
-- |   if v_caller_id is null then
-- |     raise exception 'no_active_stagerz_identity' using errcode = 'P0001';
-- |   end if;
-- |
-- |   select title into v_collab_title from public.collaborations where id = p_collaboration_id;
-- |   if v_collab_title is null then
-- |     raise exception 'collaboration_not_found' using errcode = 'P0016';
-- |   end if;
-- |
-- |   select participant_type into v_caller_type
-- |     from public.collaboration_participants
-- |     where collaboration_id = p_collaboration_id and user_id = v_caller_id;
-- |
-- |   if v_caller_type is null then
-- |     raise exception 'not_a_participant' using errcode = 'P0015';
-- |   end if;
-- |   if v_caller_type = 'owner' then
-- |     raise exception 'owner_must_transfer_before_leaving' using errcode = 'P0040';
-- |   end if;
-- |
-- |   delete from public.collaboration_participants
-- |     where collaboration_id = p_collaboration_id and user_id = v_caller_id and participant_type = 'member';
-- |   get diagnostics v_rows_changed = row_count;
-- |   if v_rows_changed <> 1 then
-- |     raise exception 'leave_failed' using errcode = 'P0041';
-- |   end if;
-- |
-- |   insert into public.collaboration_activity (collaboration_id, actor_user_id, activity_type, reference_id, metadata)
-- |     values (p_collaboration_id, v_caller_id, 'participant_left', v_caller_id, '{}'::jsonb);
-- |
-- |   insert into public.notifications (user_id, from_user_id, type, title, message, target_type, target_id)
-- |     select cp.user_id, v_caller_id, 'collaboration_participant_left', 'Participant left',
-- |            'A participant left "' || v_collab_title || '"', 'collaboration', p_collaboration_id
-- |     from public.collaboration_participants cp
-- |     where cp.collaboration_id = p_collaboration_id
-- |       and cp.user_id <> v_caller_id;
-- | end;
-- | $function$
-- |
-- <<< END function public.leave_collaboration(p_collaboration_id uuid)
--
-- ===== public.log_collaboration_asset_activity() =====
--   returns            : trigger
--   role in the app    : trigger
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres}
--   EXECUTE            : postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 1015 bytes, md5 696efad02b7f725c333f706598306e4c
--   RAISE sites        : 0
-- >>> BEGIN function public.log_collaboration_asset_activity()
-- >>> sha256 bf674957d3cc360368a995b5bdf80e8c5fee9aa260a42511d140249091ee50b4
-- | CREATE OR REPLACE FUNCTION public.log_collaboration_asset_activity()
-- |  RETURNS trigger
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_collab_title text;
-- | begin
-- |   insert into public.collaboration_activity (collaboration_id, actor_user_id, activity_type, reference_id, metadata)
-- |     values (new.collaboration_id, new.uploaded_by, 'asset_uploaded', new.id, jsonb_build_object('file_name', new.file_name));
-- |
-- |   select title into v_collab_title from public.collaborations where id = new.collaboration_id;
-- |
-- |   insert into public.notifications (user_id, from_user_id, type, title, message, target_type, target_id)
-- |     select cp.user_id, new.uploaded_by, 'collaboration_asset_uploaded', 'New asset',
-- |            'New asset "' || new.file_name || '" in "' || v_collab_title || '"', 'collaboration', new.collaboration_id
-- |     from public.collaboration_participants cp
-- |     where cp.collaboration_id = new.collaboration_id
-- |       and cp.user_id <> new.uploaded_by;
-- |
-- |   return new;
-- | end;
-- | $function$
-- |
-- <<< END function public.log_collaboration_asset_activity()
--
-- ===== public.log_collaboration_credit_activity() =====
--   returns            : trigger
--   role in the app    : trigger
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres}
--   EXECUTE            : postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 1028 bytes, md5 a6b00afd24b4e33f734411baed44f55c
--   RAISE sites        : 0
-- >>> BEGIN function public.log_collaboration_credit_activity()
-- >>> sha256 0ba25ff2bd06b301de561d687ae077fb6a6524c8aae17eb376f6de7407695947
-- | CREATE OR REPLACE FUNCTION public.log_collaboration_credit_activity()
-- |  RETURNS trigger
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_collab_title text;
-- | begin
-- |   insert into public.collaboration_activity (collaboration_id, actor_user_id, activity_type, reference_id, metadata)
-- |     values (new.collaboration_id, new.created_by, 'credit_added', new.id, jsonb_build_object('credit_role', new.credit_role, 'credited_user_id', new.user_id));
-- |
-- |   select title into v_collab_title from public.collaborations where id = new.collaboration_id;
-- |
-- |   insert into public.notifications (user_id, from_user_id, type, title, message, target_type, target_id)
-- |     select cp.user_id, new.created_by, 'collaboration_credit_added', 'New credit',
-- |            'New credit added in "' || v_collab_title || '"', 'collaboration', new.collaboration_id
-- |     from public.collaboration_participants cp
-- |     where cp.collaboration_id = new.collaboration_id
-- |       and cp.user_id <> new.created_by;
-- |
-- |   return new;
-- | end;
-- | $function$
-- |
-- <<< END function public.log_collaboration_credit_activity()
--
-- ===== public.log_collaboration_message_activity() =====
--   returns            : trigger
--   role in the app    : trigger
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres}
--   EXECUTE            : postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 954 bytes, md5 278d7269e37d2c36c6805f5eec4cd41c
--   RAISE sites        : 0
-- >>> BEGIN function public.log_collaboration_message_activity()
-- >>> sha256 f757a21b5dad83206a11f3b2e9dd48cd34b1d9de7eb4434c24a8de51f863a524
-- | CREATE OR REPLACE FUNCTION public.log_collaboration_message_activity()
-- |  RETURNS trigger
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_collab_title text;
-- | begin
-- |   insert into public.collaboration_activity (collaboration_id, actor_user_id, activity_type, reference_id, metadata)
-- |     values (new.collaboration_id, new.sender_id, 'message_posted', new.id, '{}'::jsonb);
-- |
-- |   select title into v_collab_title from public.collaborations where id = new.collaboration_id;
-- |
-- |   insert into public.notifications (user_id, from_user_id, type, title, message, target_type, target_id)
-- |     select cp.user_id, new.sender_id, 'collaboration_message_posted', 'New message',
-- |            'New message in "' || v_collab_title || '"', 'collaboration', new.collaboration_id
-- |     from public.collaboration_participants cp
-- |     where cp.collaboration_id = new.collaboration_id
-- |       and cp.user_id <> new.sender_id;
-- |
-- |   return new;
-- | end;
-- | $function$
-- |
-- <<< END function public.log_collaboration_message_activity()
--
-- ===== public.remove_collaboration_participant(p_collaboration_id uuid, p_user_id uuid) =====
--   returns            : void
--   role in the app    : frontend RPC
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres,authenticated=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 2417 bytes, md5 48503e518195cb10c4cf9d5e262daadd
--   RAISE sites        : 8  -> P0001 no_active_stagerz_identity; P0016 collaboration_not_found; P0014 collaboration_not_active; P0035 only_owner_may_remove; P0036 target_not_a_participant; P0037 cannot_remove_owner; P0038 removal_failed; P0039 cannot_leave_zero_participants
-- >>> BEGIN function public.remove_collaboration_participant(p_collaboration_id uuid, p_user_id uuid)
-- >>> sha256 ef5503e57bb02012b87b31e9ec96ad346e2e433259654c4a032aadd765243bf9
-- | CREATE OR REPLACE FUNCTION public.remove_collaboration_participant(p_collaboration_id uuid, p_user_id uuid)
-- |  RETURNS void
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_caller_id uuid := public.current_active_stagerz_user_id();
-- |   v_collab_status text;
-- |   v_collab_title text;
-- |   v_target_type text;
-- |   v_rows_changed int;
-- | begin
-- |   if v_caller_id is null then
-- |     raise exception 'no_active_stagerz_identity' using errcode = 'P0001';
-- |   end if;
-- |
-- |   select status, title into v_collab_status, v_collab_title from public.collaborations where id = p_collaboration_id;
-- |   if v_collab_status is null then
-- |     raise exception 'collaboration_not_found' using errcode = 'P0016';
-- |   end if;
-- |   if v_collab_status <> 'active' then
-- |     raise exception 'collaboration_not_active' using errcode = 'P0014';
-- |   end if;
-- |
-- |   if not public.is_collaboration_owner(p_collaboration_id) then
-- |     raise exception 'only_owner_may_remove' using errcode = 'P0035';
-- |   end if;
-- |
-- |   select participant_type into v_target_type
-- |     from public.collaboration_participants
-- |     where collaboration_id = p_collaboration_id and user_id = p_user_id;
-- |
-- |   if v_target_type is null then
-- |     raise exception 'target_not_a_participant' using errcode = 'P0036';
-- |   end if;
-- |   if v_target_type = 'owner' then
-- |     raise exception 'cannot_remove_owner' using errcode = 'P0037';
-- |   end if;
-- |
-- |   delete from public.collaboration_participants
-- |     where collaboration_id = p_collaboration_id and user_id = p_user_id and participant_type = 'member';
-- |   get diagnostics v_rows_changed = row_count;
-- |   if v_rows_changed <> 1 then
-- |     raise exception 'removal_failed' using errcode = 'P0038';
-- |   end if;
-- |
-- |   if not exists (
-- |     select 1 from public.collaboration_participants where collaboration_id = p_collaboration_id
-- |   ) then
-- |     raise exception 'cannot_leave_zero_participants' using errcode = 'P0039';
-- |   end if;
-- |
-- |   insert into public.collaboration_activity (collaboration_id, actor_user_id, activity_type, reference_id, metadata)
-- |     values (p_collaboration_id, v_caller_id, 'participant_removed', p_user_id, '{}'::jsonb);
-- |
-- |   insert into public.notifications (user_id, from_user_id, type, title, message, target_type, target_id)
-- |     values (p_user_id, v_caller_id, 'collaboration_participant_removed', 'Removed from a collaboration',
-- |             'You were removed from "' || v_collab_title || '"', 'collaboration', p_collaboration_id);
-- | end;
-- | $function$
-- |
-- <<< END function public.remove_collaboration_participant(p_collaboration_id uuid, p_user_id uuid)
--
-- ===== public.respond_to_wanted_application(p_application_id uuid, p_new_status text) =====
--   returns            : void
--   role in the app    : frontend RPC
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres,authenticated=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 4198 bytes, md5 ff172329a7c5e754fc4a2aa70f27c4a4
--   RAISE sites        : 5  -> P0001 no_active_stagerz_identity; P0009 invalid_target_status; P0010 application_not_found_or_not_pending_or_not_owned; P0014 collaboration_not_active; P0014 collaboration_not_active
-- >>> BEGIN function public.respond_to_wanted_application(p_application_id uuid, p_new_status text)
-- >>> sha256 8e25e036a97d7f39d105ecbd2631e129a4f29d2d723f7c793e6ca310bb35ab9f
-- | CREATE OR REPLACE FUNCTION public.respond_to_wanted_application(p_application_id uuid, p_new_status text)
-- |  RETURNS void
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_owner_id uuid := public.current_active_stagerz_user_id();
-- |   v_applicant_id uuid;
-- |   v_wanted_post_id uuid;
-- |   v_title text;
-- |   v_collab_id uuid;
-- |   v_collab_status text;
-- | begin
-- |   if v_owner_id is null then
-- |     raise exception 'no_active_stagerz_identity' using errcode = 'P0001';
-- |   end if;
-- |
-- |   if p_new_status not in ('accepted', 'rejected') then
-- |     raise exception 'invalid_target_status' using errcode = 'P0009';
-- |   end if;
-- |
-- |   update public.wanted_applications
-- |     set status = p_new_status, updated_at = now()
-- |     where id = p_application_id
-- |       and status = 'pending'
-- |       and exists (
-- |         select 1 from public.wanted_posts wp
-- |         where wp.id = wanted_applications.wanted_post_id
-- |           and wp.user_id = v_owner_id
-- |       )
-- |     returning applicant_id, wanted_post_id into v_applicant_id, v_wanted_post_id;
-- |
-- |   if not found then
-- |     raise exception 'application_not_found_or_not_pending_or_not_owned' using errcode = 'P0010';
-- |   end if;
-- |
-- |   select title into v_title from public.wanted_posts where id = v_wanted_post_id;
-- |
-- |   if p_new_status = 'accepted' then
-- |     select id, status into v_collab_id, v_collab_status
-- |       from public.collaborations
-- |       where wanted_post_id = v_wanted_post_id;
-- |
-- |     if v_collab_id is not null and v_collab_status <> 'active' then
-- |       raise exception 'collaboration_not_active' using errcode = 'P0014';
-- |     end if;
-- |
-- |     if v_collab_id is null then
-- |       insert into public.collaborations (wanted_post_id, title, status)
-- |         values (v_wanted_post_id, v_title, 'active')
-- |         on conflict (wanted_post_id) do nothing
-- |         returning id into v_collab_id;
-- |
-- |       if v_collab_id is null then
-- |         select id, status into v_collab_id, v_collab_status
-- |           from public.collaborations where wanted_post_id = v_wanted_post_id;
-- |         if v_collab_status <> 'active' then
-- |           raise exception 'collaboration_not_active' using errcode = 'P0014';
-- |         end if;
-- |       else
-- |         insert into public.collaboration_participants (collaboration_id, user_id, participant_type)
-- |           values (v_collab_id, v_owner_id, 'owner');
-- |
-- |         insert into public.collaboration_activity (collaboration_id, actor_user_id, activity_type, reference_id, metadata)
-- |           values (v_collab_id, v_owner_id, 'collaboration_created', v_collab_id, '{}'::jsonb);
-- |       end if;
-- |     end if;
-- |
-- |     insert into public.collaboration_participants (collaboration_id, user_id, source_application_id, participant_type)
-- |       values (v_collab_id, v_applicant_id, p_application_id, 'member');
-- |
-- |     insert into public.collaboration_activity (collaboration_id, actor_user_id, activity_type, reference_id, metadata)
-- |       values (v_collab_id, v_applicant_id, 'participant_joined', v_collab_id, '{}'::jsonb);
-- |
-- |     -- Kept as 'wanted_post' (unchanged semantics) rather than
-- |     -- upgraded to 'collaboration' -- a deliberate, conservative
-- |     -- choice: Phase 13.1 only noted this as a possible future
-- |     -- refinement, not a required change, and altering an existing
-- |     -- type's target semantics is a real behavior change beyond
-- |     -- "add the generic columns."
-- |     insert into public.notifications (user_id, from_user_id, type, title, message, wanted_post_id, wanted_application_id, read, target_type, target_id)
-- |       values (
-- |         v_applicant_id, v_owner_id,
-- |         'wanted.application.accepted',
-- |         'Application accepted',
-- |         'Your application for "' || v_title || '" was accepted',
-- |         v_wanted_post_id, p_application_id, false,
-- |         'wanted_post', v_wanted_post_id
-- |       );
-- |   else
-- |     insert into public.notifications (user_id, from_user_id, type, title, message, wanted_post_id, wanted_application_id, read, target_type, target_id)
-- |       values (
-- |         v_applicant_id, v_owner_id,
-- |         'wanted.application.rejected',
-- |         'Application rejected',
-- |         'Your application for "' || v_title || '" was rejected',
-- |         v_wanted_post_id, p_application_id, false,
-- |         'wanted_post', v_wanted_post_id
-- |       );
-- |   end if;
-- | end;
-- | $function$
-- |
-- <<< END function public.respond_to_wanted_application(p_application_id uuid, p_new_status text)
--
-- ===== public.transfer_collaboration_ownership(p_collaboration_id uuid, p_new_owner_id uuid) =====
--   returns            : void
--   role in the app    : frontend RPC
--   language           : plpgsql
--   volatility         : VOLATILE   strict: no   parallel: UNSAFE   leakproof: no
--   security           : SECURITY DEFINER
--   config             : search_path=""
--   owner              : postgres
--   ACL                : {postgres=X/postgres,service_role=X/postgres,authenticated=X/postgres}
--   EXECUTE            : authenticated (by postgres), postgres (by postgres), service_role (by postgres)
--   comment            : (none)
--   definition         : 3089 bytes, md5 18d534dbfc8f132733f46e749465a5a3
--   RAISE sites        : 9  -> P0001 no_active_stagerz_identity; P0016 collaboration_not_found; P0014 collaboration_not_active; P0024 only_owner_may_transfer; P0025 cannot_transfer_to_self; P0026 new_owner_not_a_current_member; P0027 owner_demotion_failed; P0028 owner_promotion_failed; P0029 owner_count_invariant_violated
-- >>> BEGIN function public.transfer_collaboration_ownership(p_collaboration_id uuid, p_new_owner_id uuid)
-- >>> sha256 d011a5526ee3e05862c06bdbdf7d9fe1781cd1c7d199dfe7529f9544f7066fd9
-- | CREATE OR REPLACE FUNCTION public.transfer_collaboration_ownership(p_collaboration_id uuid, p_new_owner_id uuid)
-- |  RETURNS void
-- |  LANGUAGE plpgsql
-- |  SECURITY DEFINER
-- |  SET search_path TO ''
-- | AS $function$
-- | declare
-- |   v_caller_id uuid := public.current_active_stagerz_user_id();
-- |   v_collab_status text;
-- |   v_collab_title text;
-- |   v_rows_changed int;
-- |   v_owner_count int;
-- | begin
-- |   if v_caller_id is null then
-- |     raise exception 'no_active_stagerz_identity' using errcode = 'P0001';
-- |   end if;
-- |
-- |   select status, title into v_collab_status, v_collab_title from public.collaborations where id = p_collaboration_id;
-- |   if v_collab_status is null then
-- |     raise exception 'collaboration_not_found' using errcode = 'P0016';
-- |   end if;
-- |   if v_collab_status <> 'active' then
-- |     raise exception 'collaboration_not_active' using errcode = 'P0014';
-- |   end if;
-- |
-- |   if not public.is_collaboration_owner(p_collaboration_id) then
-- |     raise exception 'only_owner_may_transfer' using errcode = 'P0024';
-- |   end if;
-- |
-- |   if p_new_owner_id = v_caller_id then
-- |     raise exception 'cannot_transfer_to_self' using errcode = 'P0025';
-- |   end if;
-- |   if not exists (
-- |     select 1 from public.collaboration_participants
-- |     where collaboration_id = p_collaboration_id and user_id = p_new_owner_id and participant_type = 'member'
-- |   ) then
-- |     raise exception 'new_owner_not_a_current_member' using errcode = 'P0026';
-- |   end if;
-- |
-- |   update public.collaboration_participants
-- |     set participant_type = 'member'
-- |     where collaboration_id = p_collaboration_id and user_id = v_caller_id and participant_type = 'owner';
-- |   get diagnostics v_rows_changed = row_count;
-- |   if v_rows_changed <> 1 then
-- |     raise exception 'owner_demotion_failed' using errcode = 'P0027';
-- |   end if;
-- |
-- |   update public.collaboration_participants
-- |     set participant_type = 'owner'
-- |     where collaboration_id = p_collaboration_id and user_id = p_new_owner_id and participant_type = 'member';
-- |   get diagnostics v_rows_changed = row_count;
-- |   if v_rows_changed <> 1 then
-- |     raise exception 'owner_promotion_failed' using errcode = 'P0028';
-- |   end if;
-- |
-- |   select count(*) into v_owner_count
-- |     from public.collaboration_participants
-- |     where collaboration_id = p_collaboration_id and participant_type = 'owner';
-- |   if v_owner_count <> 1 then
-- |     raise exception 'owner_count_invariant_violated' using errcode = 'P0029';
-- |   end if;
-- |
-- |   insert into public.collaboration_activity (collaboration_id, actor_user_id, activity_type, reference_id, metadata)
-- |     values (p_collaboration_id, v_caller_id, 'ownership_transferred', p_new_owner_id, jsonb_build_object('previous_owner', v_caller_id, 'new_owner', p_new_owner_id));
-- |
-- |   insert into public.notifications (user_id, from_user_id, type, title, message, target_type, target_id)
-- |     select cp.user_id, v_caller_id, 'collaboration_ownership_transferred', 'Ownership transferred',
-- |            'Ownership of "' || v_collab_title || '" was transferred', 'collaboration', p_collaboration_id
-- |     from public.collaboration_participants cp
-- |     where cp.collaboration_id = p_collaboration_id
-- |       and cp.user_id <> v_caller_id;
-- | end;
-- | $function$
-- |
-- <<< END function public.transfer_collaboration_ownership(p_collaboration_id uuid, p_new_owner_id uuid)

-- ---------------------------------------------------------------------
-- 3. Custom SQLSTATE contract (derived from the definitions above)
-- ---------------------------------------------------------------------
-- Every RAISE in a public function has the form
--   raise exception '<token>' using errcode = '<code>'
-- Client-facing classification and user copy: analysis/phase-21.9/error-codes.tsv.
--
-- code  | token                                             | sites | raised by
-- ------+---------------------------------------------------+-------+---------------------------------------------------------
-- P0001 | no_active_stagerz_identity                        | 20    | all 20 frontend RPCs
-- P0003 | cannot_remove_last_credential                     | 1     | admin_unmap_and_orphan_check
-- P0004 | already_mapped                                    | 1     | admin_link_recovered_account
-- P0005 | target_not_found_or_anonymized                    | 1     | admin_link_recovered_account
-- P0006 | mapping_not_found                                 | 1     | admin_unmap_and_orphan_check
-- P0007 | target_not_found                                  | 1     | admin_anonymize_account
-- P0008 | post_not_found_or_not_open_or_not_owned           | 1     | close_own_wanted_post
-- P0009 | invalid_target_status                             | 1     | respond_to_wanted_application
-- P0010 | application_not_found_or_not_pending_or_not_owned | 1     | respond_to_wanted_application
-- P0011 | wanted_post_not_found                             | 1     | create_wanted_application
-- P0012 | wanted_post_not_open                              | 1     | create_wanted_application
-- P0013 | cannot_apply_to_own_wanted                        | 1     | create_wanted_application
-- P0014 | collaboration_not_active                          | 17    | 16 functions (listed below)
-- P0015 | not_a_participant                                 | 8     | 8 functions (listed below)
-- P0016 | collaboration_not_found                           | 10    | 10 functions (listed below)
-- P0017 | title_empty                                       | 2     | create_collaboration_task, edit_collaboration_task
-- P0018 | title_too_long                                    | 2     | create_collaboration_task, edit_collaboration_task
-- P0019 | assignee_not_a_participant                        | 1     | create_collaboration_task
-- P0020 | task_not_found                                    | 3     | 3 functions (listed below)
-- P0021 | task_not_found_or_not_todo                        | 1     | complete_collaboration_task
-- P0022 | only_owner_may_change_status                      | 1     | change_collaboration_status
-- P0023 | invalid_status_transition                         | 1     | change_collaboration_status
-- P0024 | only_owner_may_transfer                           | 1     | transfer_collaboration_ownership
-- P0025 | cannot_transfer_to_self                           | 1     | transfer_collaboration_ownership
-- P0026 | new_owner_not_a_current_member                    | 1     | transfer_collaboration_ownership
-- P0027 | owner_demotion_failed                             | 1     | transfer_collaboration_ownership
-- P0028 | owner_promotion_failed                            | 1     | transfer_collaboration_ownership
-- P0029 | owner_count_invariant_violated                    | 1     | transfer_collaboration_ownership
-- P0030 | only_owner_may_invite                             | 1     | invite_collaboration_participant
-- P0031 | cannot_invite_self                                | 1     | invite_collaboration_participant
-- P0032 | target_user_not_found                             | 1     | invite_collaboration_participant
-- P0033 | target_user_not_eligible                          | 1     | invite_collaboration_participant
-- P0034 | already_a_participant                             | 1     | invite_collaboration_participant
-- P0035 | only_owner_may_remove                             | 1     | remove_collaboration_participant
-- P0036 | target_not_a_participant                          | 1     | remove_collaboration_participant
-- P0037 | cannot_remove_owner                               | 1     | remove_collaboration_participant
-- P0038 | removal_failed                                    | 1     | remove_collaboration_participant
-- P0039 | cannot_leave_zero_participants                    | 1     | remove_collaboration_participant
-- P0040 | owner_must_transfer_before_leaving                | 1     | leave_collaboration
-- P0041 | leave_failed                                      | 1     | leave_collaboration
-- P0042 | only_creator_or_owner_may_delete                  | 1     | delete_collaboration_task
-- P0043 | delete_failed                                     | 2     | delete_collaboration_asset, delete_collaboration_task
-- P0044 | asset_not_found                                   | 2     | delete_collaboration_asset, edit_collaboration_asset
-- P0045 | asset_pending_deletion                            | 2     | delete_collaboration_asset, edit_collaboration_asset
-- P0046 | only_uploader_or_owner_may_delete                 | 1     | delete_collaboration_asset
-- P0047 | credit_not_found                                  | 2     | delete_collaboration_credit, edit_collaboration_credit
-- P0048 | only_owner_may_edit_credit                        | 1     | edit_collaboration_credit
-- P0049 | role_empty                                        | 2     | create_collaboration_credit, edit_collaboration_credit
-- P0050 | only_owner_may_delete_credit                      | 1     | delete_collaboration_credit
-- P0051 | delete_failed                                     | 1     | delete_collaboration_credit
-- P0052 | credited_user_not_a_participant                   | 1     | create_collaboration_credit
-- P0053 | duplicate_credit                                  | 1     | create_collaboration_credit
-- P0054 | message_not_found                                 | 2     | delete_collaboration_message, edit_collaboration_message
-- P0055 | only_author_may_edit_message                      | 1     | edit_collaboration_message
-- P0056 | body_empty                                        | 2     | create_collaboration_message, edit_collaboration_message
-- P0057 | body_too_long                                     | 2     | create_collaboration_message, edit_collaboration_message
-- P0058 | only_author_or_owner_may_delete_message           | 1     | delete_collaboration_message
-- P0059 | delete_failed                                     | 1     | delete_collaboration_message
--
-- Codes raised by more than two functions:
--   P0014: complete_collaboration_task, create_collaboration_credit,
--          create_collaboration_message, create_collaboration_task,
--          delete_collaboration_asset, delete_collaboration_credit,
--          delete_collaboration_message, delete_collaboration_task,
--          edit_collaboration_asset, edit_collaboration_credit,
--          edit_collaboration_message, edit_collaboration_task,
--          invite_collaboration_participant, remove_collaboration_participant,
--          respond_to_wanted_application, transfer_collaboration_ownership
--   P0015: complete_collaboration_task, create_collaboration_credit,
--          create_collaboration_message, create_collaboration_task,
--          edit_collaboration_asset, edit_collaboration_message,
--          edit_collaboration_task, leave_collaboration
--   P0016: change_collaboration_status, create_collaboration_credit,
--          create_collaboration_message, create_collaboration_task,
--          delete_collaboration_message, edit_collaboration_message,
--          invite_collaboration_participant, leave_collaboration,
--          remove_collaboration_participant, transfer_collaboration_ownership
--   P0020: complete_collaboration_task, delete_collaboration_task,
--          edit_collaboration_task
--
-- Non-RAISE SQLSTATEs the frontend branches on: 23505 (unique_violation) --
-- from wanted_applications_wanted_post_id_applicant_id_key (applyToWanted)
-- and users_username_key (saveProfile); see schema.sql.
